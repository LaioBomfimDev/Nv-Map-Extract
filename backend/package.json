{
  "name": "friendly-miner-backend",
  "version": "1.0.0",
  "description": "Backend amigável para o ecossistema Friendly Miner",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js --watch src --ext js,json",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "format": "prettier --write src/**/*.js",
    "setup": "npm install && node scripts/setup-database.js",
    "clean": "rm -rf logs/* temp/* && npm run lint:fix",
    "optimize": "npm run lint:fix && npm run test && npm run clean",
    "production": "NODE_ENV=production node src/server.js",
    "analyze": "npm audit && npm outdated",
    "backup": "node scripts/backup-database.js",
    "migrate": "node scripts/migrate-database.js"
  },
  "keywords": [
    "google-maps",
    "scraper",
    "dashboard",
    "api",
    "express",
    "sqlite"
  ],
  "author": "Dashboard Personalizado",
  "license": "MIT",
  "dependencies": {
    "chokidar": "^3.5.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "lodash": "^4.17.21",
    "moment": "^2.29.4",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^22.15.0",
    "sqlite3": "^5.1.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
