# Sem default embutido em produção: a URL do backend é configurada em
# runtime pelo modal "Configuração da API" (salva em localStorage).
# Deixe vazio para que o app peça a configuração no primeiro acesso (mobile).
REACT_APP_API_URL=
